Run the Unearth data model skill to discover all entities, storage mechanisms, and relationships.

Read `skills/07-data-model/SKILL.md` and follow its process exactly. This skill discovers storage entities, projections/DTOs, view models, entity attributes, relationships (explicit and implied), cross-project usage, capability alignment, and coupling hotspots.

Prerequisites: `outputs/06-capabilities.md`, `outputs/05-components-*.md`, and `outputs/03-external-systems.md` must exist. If they don't, run the upstream skills first.

After completing the skill:
1. Save the output to `outputs/07-data-model.md` following the exact output format.
2. Run through the verification checklist.
3. The remaining Stage 3 skills (patterns and dependencies) can be run next. Suggest continuing with patterns detection, then dependencies.
