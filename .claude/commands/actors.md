Run the Unearth actors and boundaries skill on this codebase.

Read `skills/02-actors-and-boundaries/SKILL.md` and follow its process exactly. This skill identifies all human and system actors from authentication patterns, draws the system boundary, and maps actor-to-system relationships.

Prerequisites: `outputs/00-recon.md` and `outputs/01-entry-points.md` must exist. If they don't, run `/recon` then `/entry-points` first.

After completing the skill:
1. Save the output to `outputs/02-actors-and-boundaries.md` following the exact output format in the skill.
2. Run through the verification checklist.
3. Note: This completes Stage 1 (Reconnaissance). The system context is now understood.
4. Suggest running `/containers` to begin Stage 2 (Structure).
