Run the Unearth entry points skill on this codebase.

Read `skills/01-entry-points/SKILL.md` and follow its process exactly. This skill discovers every way into the system — HTTP routes, event triggers, scheduled jobs, message handlers, WebSocket hubs, and background workers.

Prerequisites: `outputs/00-recon.md` must exist. If it doesn't, run `/recon` first.

After completing the skill:
1. Save the output to `outputs/01-entry-points.md` following the exact output format in the skill.
2. Run through the verification checklist.
3. Suggest running `/actors` as the next step.
